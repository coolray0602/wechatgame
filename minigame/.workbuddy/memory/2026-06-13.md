# 2026-06-13 工作記錄

## 道具商店購買系統

- **移除**：舊的免費次數系統（`shuffleRemainingCount` / `throwBackRemainingCount`）
- **新增**：`ownedItems[]` 陣列（最多 5 個道具）
- **商店**：左側 `images/shop.png`，右側 `images/shelf.png`（5 格道具櫃）
- **商品**：`wash.png`（洗牌，100 金幣）、`throw.png`（丟回，30 金幣）
- **購物邏輯**：`buyItem()` 檢查金幣是否足夠、道具櫃是否已滿，扣金幣後加入 `ownedItems`
- **使用邏輯**：點擊 shelf 格子使用道具，從 `ownedItems` 移除
- **金幣**：即得分（`totalScore + currentRoundScore`），三消得 10 金幣
- **刪除**：`onTouchEnd` 函數（不再需要）

## 其他改動
- 底部區域從兩個按鈕改為商店+道具櫃佈局
- 加載 4 張新圖片：shop.png, shelf.png, wash.png, throw.png

## 購物面板

- **觸發**：點擊底部 shop 圖標打開
- **面板**：螢幕中央 3×3 格佈局（9 格，目前 2 種商品）
- **遮罩**：半透明黑色背景，區塊外點擊關閉
- **商品顯示**：道具圖標 + 名稱 + 💰價格
- **開放時**：`shopOpen` 狀態阻擋其他遊戲互動（卡片/設定等）
- **Shelf 加大**：高度從 `shopSize * 0.65` → `shopSize * 1.2`
- 移除底部小型 wash/throw 商品（改到購物面板內顯示）
